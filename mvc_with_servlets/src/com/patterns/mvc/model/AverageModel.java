package com.patterns.mvc.model;

//to create the project: new->file->dynamic web project

//model: to perform action 
public class AverageModel {
	
	public int calculateAverage(int num1, int num2) {
		return (num1 + num2) / 2;
	}

}
